# `synapse feat`

Alias of `workflow`.

## Usage

```bash
synapse feat [--task-type <frontend|backend|fullstack>] [--yes] <request...>
```

## Notes

- Equivalent to `synapse workflow ...` (see `references/workflow.md`).
